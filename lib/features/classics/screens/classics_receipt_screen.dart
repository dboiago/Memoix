import 'package:flutter/material.dart';

class ClassicsReceiptScreen extends StatelessWidget {
  const ClassicsReceiptScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Receipt'),
      ),
    );
  }
}
